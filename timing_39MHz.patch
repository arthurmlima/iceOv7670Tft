--- a/Makefile
+++ b/Makefile
@@ -2,7 +2,7 @@
 PCF      := icebreaker.pcf
 DEVICE   := up5k
 PACKAGE  := sg48
-FREQ     := 39.75
+FREQ     := 39.00
 
 SOURCES  := icebreaker_st7789_top.v \
             cam_init.v cam_capture.v pixel_fifo.v \
--- a/icebreaker_st7789_top.v
+++ b/icebreaker_st7789_top.v
@@ -4,14 +4,14 @@
 // icebreaker_st7789_top.v
 //
 // OV7670 -> 256x16 FIFO -> ST7789, no CPU and no external framebuffer.
-// The display wiring and 39.75 MHz PLL are retained from the working test
+// The display wiring and 39.00 MHz PLL are retained from the working test
 // pattern project. Camera timing is deliberately slowed to match the proven
-// 9.9375 MHz SPI interface.
+// 9.75 MHz SPI interface.
 // ============================================================================
 module icebreaker_st7789_top #(
     parameter integer USE_PLL        = 1,
-    parameter integer SYS_CLK_HZ     = 39750000,
-    parameter integer SPI_HZ         = 9937500,
+    parameter integer SYS_CLK_HZ     = 39000000,
+    parameter integer SPI_HZ         = 9750000,
     parameter integer POR_MS         = 10,
     parameter integer BL_ACTIVE_HIGH = 1
 )(
@@ -46,11 +46,11 @@
     generate
         if (USE_PLL != 0) begin : g_pll
             wire pll_clk;
-            // 12 MHz * (52+1) / 2^4 = 39.75 MHz
+            // 12 MHz * (51+1) / 2^4 = 39.00 MHz
             SB_PLL40_PAD #(
                 .FEEDBACK_PATH("SIMPLE"),
                 .DIVR(4'b0000),
-                .DIVF(7'b0110100),
+                .DIVF(7'b0110011),
                 .DIVQ(3'b100),
                 .FILTER_RANGE(3'b001)
             ) pll (
@@ -97,7 +97,7 @@
             cam_xclk_q <= ~cam_xclk_q;
     end
 
-    assign cam_xclk  = cam_xclk_q;  // 19.875 MHz
+    assign cam_xclk  = cam_xclk_q;  // 19.500 MHz
     assign cam_rst_n = resetn;
     assign cam_pwdn  = 1'b0;
 
@@ -106,7 +106,7 @@
     wire cam_cfg_done;
 
     cam_init #(
-        .TICK_DIV   (100),
+        .TICK_DIV   (98),
         .BOOT_TICKS (4000),
         .GAP_TICKS  (800),
         .RST_TICKS  (4000)
--- a/st7789_camera_ctrl.v
+++ b/st7789_camera_ctrl.v
@@ -13,8 +13,8 @@
 // SCLK may pause while the FIFO is empty; CS stays low and RAMWR remains active.
 // ============================================================================
 module st7789_camera_ctrl #(
-    parameter integer CLK_HZ       = 39750000,
-    parameter integer SPI_HZ       = 9937500,
+    parameter integer CLK_HZ       = 39000000,
+    parameter integer SPI_HZ       = 9750000,
     parameter integer WIDTH        = 280,
     parameter integer HEIGHT       = 240,
     parameter integer X_SHIFT      = 20,
--- a/cam_init.v
+++ b/cam_init.v
@@ -20,10 +20,10 @@
 // Register table = the proven camera.h set, with exactly these deltas:
 //
 //   COM7  0x04 -> 0x14   select QVGA as well as RGB
-//   CLKRC 0x00 -> 0x05   internal clock = XCLK/6 = 3.3125 MHz
+//   CLKRC 0x00 -> 0x05   internal clock = XCLK/6 = 3.250 MHz
 //   DBLV  0x4A -> 0x0A   PLL x4 OFF (x4 would quadruple everything)
 //   COM3  0x00 -> 0x04   enable DCW              \
-//   COM14 0x00 -> 0x19   manual scaling, PCLK/2   > QVGA, PCLK 1.65625 MHz
+//   COM14 0x00 -> 0x19   manual scaling, PCLK/2   > QVGA, PCLK 1.625 MHz
 //   + 0x70..0x73, 0xA2   canonical scaling regs  /
 //   RGB444 0x03 -> 0x00  RGB444 OFF  \  the old table left the sensor in
 //   COM15  0xF0 -> 0xD0  true RGB565 /  444/555 mode; the panel needs 565
@@ -33,7 +33,7 @@
 // ============================================================================
 
 module cam_init #(
-    parameter TICK_DIV   = 100,    // 39.75 MHz / (4 * ~99.4 kHz) quarter tick
+    parameter TICK_DIV   = 98,     // 39.00 MHz / 98 = 397.96 kHz quarter tick
     parameter BOOT_TICKS = 4000,   // ~10 ms after reset before first write
     parameter GAP_TICKS  = 800,    // ~2 ms between writes
     parameter RST_TICKS  = 4000    // ~10 ms settle after COM7 reset writes
--- a/cam_capture.v
+++ b/cam_capture.v
@@ -1,10 +1,10 @@
 `timescale 1ns / 1ps
 `default_nettype none
 // ============================================================================
-// cam_capture.v - OV7670 RGB565 receiver in the 39.75 MHz system domain.
+// cam_capture.v - OV7670 RGB565 receiver in the 39.00 MHz system domain.
 //
-// PCLK is treated as data, not as a clock.  With XCLK=19.875 MHz, CLKRC=/6,
-// and COM14 PCLK=/2, PCLK is about 1.65625 MHz.  That leaves 24 system-clock
+// PCLK is treated as data, not as a clock.  With XCLK=19.500 MHz, CLKRC=/6,
+// and COM14 PCLK=/2, PCLK is about 1.625 MHz.  That leaves 24 system-clock
 // periods per PCLK period, so 2-FF synchronization and edge detection provide
 // a generous sampling window without an asynchronous clock domain.
 //
--- a/spi_stream_tx.v
+++ b/spi_stream_tx.v
@@ -11,8 +11,8 @@
 // extra idle SCLK period.  DC is latched with each byte and therefore changes
 // only at byte boundaries.
 //
-// SCLK = CLK_HZ / (2 * HALF_DIV).  With CLK_HZ=39.75 MHz and HALF_DIV=2,
-// SCLK is exactly 9.9375 MHz.
+// SCLK = CLK_HZ / (2 * HALF_DIV).  With CLK_HZ=39.00 MHz and HALF_DIV=2,
+// SCLK is exactly 9.750 MHz.
 // ============================================================================
 module spi_stream_tx #(
     parameter integer HALF_DIV = 2
--- a/timing_check.py
+++ b/timing_check.py
@@ -1,8 +1,8 @@
 #!/usr/bin/env python3
 """Clock and rate-matching calculation for the OV7670/ST7789 design."""
 
-SYS_HZ = 39_750_000
-SPI_HZ = 9_937_500
+SYS_HZ = 39_000_000
+SPI_HZ = 9_750_000
 XCLK_HZ = SYS_HZ / 2
 CLKRC_DIV = 6
 CAM_INT_HZ = XCLK_HZ / CLKRC_DIV
